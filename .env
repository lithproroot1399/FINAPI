# Dependências do Docker (opcional)
# Se estiver usando Docker, execute antes de rodar o servidor:
# docker-compose up -d

# Variáveis de Ambiente para Desenvolvimento
DB_HOST=localhost
DB_PORT=5432
DB_NAME=finapi_db
DB_USER=postgres
DB_PASSWORD=postgres
NODE_ENV=development
PORT=3333
